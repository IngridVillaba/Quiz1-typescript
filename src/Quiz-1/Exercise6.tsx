// 6. API de contexto con TypeScript: configure un contexto de React para un tema (por ejemplo, oscuro o claro) con un componente de 
//proveedor y consumidor correspondiente, escrito con TypeScript. 
// Esto le ayudará a practicar el uso de genéricos y el contexto de escritura.


import React from "react";

